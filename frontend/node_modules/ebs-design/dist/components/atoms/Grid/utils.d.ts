import { Grid } from './interface';
export declare const isNil: (value: any) => boolean;
export declare const getClassName: ({ offset, g, gx, gy }: Grid) => {
    [key: string]: boolean | undefined;
};
