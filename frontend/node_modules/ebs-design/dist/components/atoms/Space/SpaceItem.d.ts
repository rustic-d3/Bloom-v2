import * as React from 'react';
import { SpaceDirection } from './Space';
export interface ItemProps {
    index: number;
    direction?: SpaceDirection;
    wrap?: boolean;
}
export declare const SpaceItem: React.FC<ItemProps>;
