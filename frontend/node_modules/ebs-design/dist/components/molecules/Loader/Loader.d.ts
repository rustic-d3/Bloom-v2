import * as React from 'react';
import { LoaderSpinnerProps } from './LoaderSpinner';
export declare type SpinnerSize = 'small' | 'middle' | 'regular';
export interface LoaderComposition {
    Inline: React.FC;
    Spinner: React.FC<LoaderSpinnerProps>;
}
export interface LoaderProps extends React.HTMLAttributes<HTMLDivElement> {
    fade?: boolean;
    size?: SpinnerSize;
    loading: boolean;
    fixed?: boolean;
    height?: number | string;
}
declare const Loader: React.FC<LoaderProps> & LoaderComposition;
export { Loader };
