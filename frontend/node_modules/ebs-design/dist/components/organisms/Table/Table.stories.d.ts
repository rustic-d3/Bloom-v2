import * as React from 'react';
import { TableProps } from './Table';
import { GenericObject } from 'types';
declare const _default: {
    title: string;
    component: <T extends object>({ size, emptyCell, children, ...props }: TableProps<T>) => React.ReactElement<any, string | ((props: any) => React.ReactElement<any, any> | null) | (new (props: any) => React.Component<any, any, any>)>;
    argTypes: {
        emptyCell: {
            control: string;
        };
    };
};
export default _default;
export declare const Regular: React.FC<TableProps<GenericObject>> & {
    args: TableProps<GenericObject>;
};
export declare const WithHeadChildrens: React.FC<TableProps<GenericObject>> & {
    args: TableProps<GenericObject>;
};
