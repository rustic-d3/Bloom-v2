import * as React from 'react';
interface Props {
    sizes?: string[];
    defaultSize?: string;
    children: (size: string) => React.ReactNode;
}
declare const SizeSwitcher: React.FC<Props>;
export default SizeSwitcher;
